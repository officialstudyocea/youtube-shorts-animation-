{
  "name": "youtube-shorts-automation",
  "version": "1.0.0",
  "description": "YouTube Shorts Automation - Root",
  "scripts": {
    "postinstall": "cd server && npm install",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
